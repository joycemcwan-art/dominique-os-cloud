Dominique OS static deployment bundle.

This folder is safe to publish as static files only when cloud-config.js has
enabled=true and contains the Supabase URL + publishable key.

This bundle intentionally contains only browser assets and public Supabase
configuration.
